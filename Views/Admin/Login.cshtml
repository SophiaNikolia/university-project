@model Admin

<div class="d-flex justify-content-center mt-4 h-50-percent">
    <form method="post" class="d-flex flex-column w-75 w-md-50 w-lg-25 border px-lg-5 px-3 shadow">
        <div class="mt-3 mb-4 text-center">
            <h2>Admin Login</h2>
        </div>

        <div asp-validation-summary="ModelOnly" class="login-error text-danger"></div>

        <label asp-for="@Model.Email" class="form-label">Username</label>
        <input asp-for="@Model.Email" type="text" class="form-control">

        <div class="error-message text-danger">
            <span asp-validation-for="@Model.Email"></span>
        </div>
        
        <label asp-for="@Model.Password" class="form-label mt-3">Password</label>
        <div class="input-group">
            <input asp-for="@Model.Password" id="passwordInput" type="password" autocomplete="email" class="form-control border-end-0" />
            <span id="eyeSlashIcon" class="input-group-text bg-transparent rounded-end hovered"><i class="fa-solid fa-eye-slash"></i></span>
            <span id="eyeIcon" class="visually-hidden input-group-text bg-transparent hovered"><i class="fa-solid fa-eye"></i></span>
        </div>
        <div class="error-message text-danger">
            <span asp-validation-for="@Model.Password"></span>
        </div>
        <button type="submit" class="btn btn-primary mt-5">Sign In</button>
    </form>
</div>
